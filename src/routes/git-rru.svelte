<svelte:head>
    <title>git rru | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-rru">git rru</h1>
<h2
id="remote-update-i.e.-fetch-updates-for-a-named-set-of-remotes-in-the-repository-as-defined-by-remotes">remote
update i.e. fetch updates for a named set of remotes in the repository
as defined by remotes</h2>
<p>Git alias:</p>
<pre class="git"><code>rru = remote update</code></pre>
<p>Example:</p>
<pre class="shell"><code>git rru</code></pre>

</div>
