<svelte:head>
    <title>git cam | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-cam">git cam</h1>
<h2 id="amend-the-tip-of-the-current-branch-edit-the-message">Amend the
tip of the current branch; edit the message</h2>
<p>Git alias:</p>
<pre class="git"><code>cam = commit --amend --message</code></pre>
<p>Example:</p>
<pre class="shell"><code>git cam</code></pre>

</div>
