<svelte:head>
    <title>git ma | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-ma">git ma</h1>
<h2 id="merge-abort-i.e.-abort-the-merge-process">Merge abort i.e. abort
the merge process</h2>
<p>Git alias:</p>
<pre class="git"><code>ma = merge --abort</code></pre>
<p>Example:</p>
<pre class="shell"><code>git ma</code></pre>

</div>
