<svelte:head>
    <title>git grep-group | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-grep-group">git grep-group</h1>
<h2 id="find-text-and-group-the-output-lines-a.k.a.-gg">Find text and
group the output lines, a.k.a. <code>gg</code></h2>
<p>Git alias:</p>
<pre class="git"><code>grep-group = grep --break --heading --line-number --color</code></pre>
<p>Syntax:</p>
<pre class="shell"><code>git grep-group [options]</code></pre>
<p>Example:</p>
<pre class="shell"><code>git grep-group foo</code></pre>

</div>
