<svelte:head>
    <title>git ap | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-ap">git ap</h1>
<h2 id="short-for-git-add-patch---add-by-patch">Short for “git add
–patch” - Add by patch</h2>
<p>Git alias:</p>
<pre class="git"><code>ap = add --patch</code></pre>
<p>Example:</p>
<pre class="shell"><code>git ap</code></pre>
<p>Show each change, and ask if we want to add it in the repo.</p>

</div>
