<svelte:head>
    <title>git panic | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-panic">git panic</h1>
<h2
id="when-youre-a-little-worried-that-the-world-is-coming-to-an-end">When
you’re a little worried that the world is coming to an end</h2>
<p>Git alias:</p>
<pre class="git"><code>panic = !tar cvf ../panic.tar *</code></pre>
<p>Example:</p>
<pre class="shell"><code>git panic</code></pre>
<p>This alias creates an archive of the entire repo.</p>

</div>
