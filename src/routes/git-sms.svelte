<svelte:head>
    <title>git sms | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-sms">git sms</h1>
<h2
id="submodule-sync-i.e.-synchronize-a-submodule-in-this-repo">Submodule
sync i.e. synchronize a submodule in this repo</h2>
<p>Git alias:</p>
<pre class="git"><code>sms = submodule sync</code></pre>
<p>Example:</p>
<pre class="shell"><code>git sms foo</code></pre>

</div>
