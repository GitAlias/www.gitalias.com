<svelte:head>
    <title>git bnm | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-bnm">git bnm</h1>
<h2
id="list-branches-whose-tips-are-not-reachable-from-the-specified-commit-head-if-not-specified">List
branches whose tips are not reachable from the specified commit (HEAD if
not specified)</h2>
<p>Git alias:</p>
<pre class="git"><code>bnm = branch --no-merged</code></pre>
<p>Compare <a href="../git-bm">bm</a></p>

</div>
