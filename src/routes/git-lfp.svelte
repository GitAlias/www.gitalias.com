<svelte:head>
    <title>git lfp | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-lfp">git lfp</h1>
<h2 id="log-with-first-parent">Log with first parent</h2>
<p>Git alias:</p>
<pre class="git"><code>lfp = log --first-parent</code></pre>
<p>Example:</p>
<pre class="shell"><code>git lfp</code></pre>
<p>This alias can be useful for teamwork for a branch that only accepts
pull requests.</p>

</div>
