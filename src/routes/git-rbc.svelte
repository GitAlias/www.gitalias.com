<svelte:head>
    <title>git rbc | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-rbc">git rbc</h1>
<h2 id="rebase-continue-i.e.-continue-the-rebase-process">Rebase
continue i.e. continue the rebase process</h2>
<p>Git alias:</p>
<pre class="git"><code># git rbc: rebase - resolution.
rbc = rebase --continue</code></pre>
<p>Example:</p>
<pre class="shell"><code>git rbc</code></pre>
<p>Continue after resolving a conflict and updating the index.</p>

</div>
