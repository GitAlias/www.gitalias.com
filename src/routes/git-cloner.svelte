<svelte:head>
    <title>git cloner | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-cloner">git cloner</h1>
<h2
id="clone-a-repository-using-our-recommended-way-i.e.-recurse-on-all-submodules">Clone
a repository using our recommended way i.e. recurse on all
submodules</h2>
<p>Git alias:</p>
<pre class="git"><code>cloner = clone --recurse-submodules</code></pre>
<p>Example:</p>
<pre class="shell"><code>git cloner</code></pre>

</div>
