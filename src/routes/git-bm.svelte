<svelte:head>
    <title>git bm | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-bm">git bm</h1>
<h2
id="list-branches-whose-tips-are-reachable-from-the-specified-commit-head-if-not-specified">List
branches whose tips are reachable from the specified commit (HEAD if not
specified)</h2>
<p>Git alias:</p>
<pre class="git"><code>bm = branch --merged</code></pre>

</div>
