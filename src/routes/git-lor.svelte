<svelte:head>
    <title>git lor | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-lor">git lor</h1>
<h2
id="log-with-one-line-per-item-in-reverse-order-i.e.-recent-items-first">Log
with one line per item, in reverse order i.e. recent items first</h2>
<p>Git alias:</p>
<pre class="git"><code>lor = log --oneline --reverse</code></pre>
<p>Example:</p>
<pre class="shell"><code>git lor</code></pre>

</div>
