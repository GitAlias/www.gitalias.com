<svelte:head>
    <title>git diff-deep | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-diff-deep">git diff-deep</h1>
<h2
id="diff-with-deep-information-using-our-preferred-options-a.k.a.-dd">Diff
with deep information using our preferred options, a.k.a.
<code>dd</code></h2>
<p>Git alias:</p>
<pre class="git"><code>diff-deep = diff --check --dirstat --find-copies --find-renames --histogram --color</code></pre>
<p>Example:</p>
<pre class="shell"><code>git diff-deep</code></pre>

</div>
