<svelte:head>
    <title>git initer | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-initer">git initer</h1>
<h2
id="initalize-a-repo-using-our-recommended-way-i.e.-with-an-empty-rebaseable-commit">Initalize
a repo using our recommended way i.e. with an empty rebaseable
commit</h2>
<p>Git alias:</p>
<pre class="git"><code>initer = init-empty</code></pre>
<p>Example:</p>
<pre class="shell"><code>git initer</code></pre>
<p>See <a href="../git-init-empty">git init-empty</a>.</p>
<p>This initialization makes it easier to do later git rebase commands,
because it enables a rebase to go all the way back to the first
commit.</p>

</div>
