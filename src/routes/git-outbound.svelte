<svelte:head>
    <title>git outbound | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-outbound">git outbound</h1>
<h2 id="show-outgoing-changes-compared-to-upstream">Show outgoing
changes compared to upstream</h2>
<p>Git alias:</p>
<pre class="git"><code># git outbound - Show outgoing changes compared to upstream.
outbound = log @&lbrace;upstream&rbrace;..</code></pre>
<p>Example:</p>
<pre class="shell"><code>git outbound</code></pre>
<p>Compare <a href="../git-inbound">git inbound</a>.</p>

</div>
