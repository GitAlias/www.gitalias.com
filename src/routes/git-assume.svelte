<svelte:head>
    <title>git assume | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-assume">git assume</h1>
<h2 id="assume-files-are-unchanged">Assume files are unchanged</h2>
<p>Git alias:</p>
<pre class="git"><code>assume = update-index --assume-unchanged</code></pre>
<p>Example:</p>
<pre class="shell"><code>git assume</code></pre>
<p>To see how this works, see <a
href="../git-assume-and-git-unassume">git assume-* &amp; git
unassume-*</a></p>

</div>
