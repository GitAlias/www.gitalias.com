<svelte:head>
    <title>git ca | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-ca">git ca</h1>
<h2
id="amend-the-tip-of-the-current-branch-do-not-create-a-new-commit">Amend
the tip of the current branch; do not create a new commit</h2>
<p>Git alias:</p>
<pre class="git"><code>ca = commit --amend</code></pre>
<p>Example:</p>
<pre class="shell"><code>git ca</code></pre>

</div>
