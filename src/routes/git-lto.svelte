<svelte:head>
    <title>git lto | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-lto">git lto</h1>
<h2 id="log-with-items-appearing-in-topological-order">Log with items
appearing in topological order</h2>
<p>Git alias:</p>
<pre class="git"><code># git lto:
lto = log --topo-order</code></pre>
<p>Example:</p>
<pre class="shell"><code>git lto</code></pre>
<p>Topological order means that descendant commits are shown before
their parents.</p>

</div>
