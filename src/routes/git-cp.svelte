<svelte:head>
    <title>git cp | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-cp">git cp</h1>
<h2
id="cherry-pick---apply-the-changes-introduced-by-some-existing-commits">Cherry-pick
- apply the changes introduced by some existing commits;</h2>
<p>Git alias:</p>
<pre class="git"><code>cp = cherry-pick</code></pre>
<p>Example:</p>
<pre class="shell"><code>git cp</code></pre>
<p>Cherry pick is useful for moving small chunks of code between
branches.</p>

</div>
