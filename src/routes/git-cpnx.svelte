<svelte:head>
    <title>git cpnx | GitAlias.com</title>
</svelte:head>

<div class="prose">

<h1 id="git-cpnx">git cpnx</h1>
<h2 id="cherry-pick-with-no-commit-and-with-explanation">Cherry-pick
with no commit and with explanation</h2>
<p>Git alias:</p>
<pre class="git"><code>cpnx = cherry-pick -n -x</code></pre>
<p>Example:</p>
<pre class="shell"><code>git cpnx</code></pre>
<p>Cherry-pick with without making a commit.</p>
<p>When when recording the commit, append a line that says “(cherry
picked from commit …)”</p>

</div>
